import 'package:flutter/material.dart';

class MenuviewExample extends StatelessWidget {
  const MenuviewExample({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
